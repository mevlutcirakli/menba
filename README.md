# Menba

Kurulabilir build almak icin:

- Android APK (preview): `npm run build:android:preview`
- iOS ad-hoc benzeri internal preview: `npm run build:ios:preview`
- Tum platformlar (preview): `npm run build:all:preview`
